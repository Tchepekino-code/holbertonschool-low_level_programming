#include "main.h"

/**
 * add - adds two integers and returns the result
 * @i: the first integer
 * @j: the second integer
 *
 * Return: the sum of a and b
 */

int add(int i, int j)
{
return (i + j);
}
