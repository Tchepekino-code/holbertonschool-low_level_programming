#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @r: The character to check
 *
 * Return: absolute value
 */
int _abs(int)
{
int i;
if (i >= 0)
 {
 _abs = i;
 }
 else {
 i = -i;
}
return (0)
}
