#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @r: The character to check
 *
 * Return: absolute value
 */
int _abs(int i)
{
if (i >= 0)
{
return i;
}
else {
return -i;
}
}
