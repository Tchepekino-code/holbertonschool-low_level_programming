#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  for (char c = 'a'; c <= 'z'; c++)
    putchar(c);
  for (char b = 'A'; b <= 'Z'; b++)
    putchar(b);
  putchar('\n')

  return (0)
}
